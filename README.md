# shazam-to-youtube

## SSL Certificate (Windows)

Το repo περιλαμβάνει `certs/cacert.pem` για την επίλυση SSL verification
errors σε Windows PHP installations. Δεν χρειάζεται επιπλέον ρύθμιση —
το Laravel HTTP client το χρησιμοποιεί αυτόματα.
